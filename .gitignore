# macOS
.DS_Store

# Personal scratch todo list — not part of the repo's tracked docs
TODO.md

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.coverage

# Node
node_modules/
dist/

# Env
.env
.env.local
.env.aikido

# Postman desktop app local workspace state (created by "Work locally with
# Git" mode) — not part of the generated/hand-maintained collection+envs
postman/.postman/
postman/postman/

# Postman environments hold real secret values (bearerToken) once filled in —
# copy the committed *.example templates locally, never commit the real files
postman/local.postman_environment.json
postman/production.postman_environment.json

# mutmut working copy
backend/mutants/
