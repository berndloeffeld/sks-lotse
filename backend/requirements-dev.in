# Dev/CI tooling on top of the runtime dependencies. The constraint keeps every runtime package at
# exactly the version requirements.txt locks, so dev and production can't resolve differently.
-c requirements.txt
-r requirements.in
pytest==9.1.1
pytest-cov==7.1.0
httpx==0.28.1
mypy==2.3.1
ruff==0.16.8
pre-commit==4.6.2
pymupdf==1.28.2
pip-tools==7.6.1
