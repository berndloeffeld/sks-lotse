# Direct runtime dependencies. Edit this file, never requirements.txt — that one is the full,
# hash-pinned lock Render and CI install from, regenerated by ./scripts/lock_backend_requirements.sh
# (README → Dependencies).
fastapi==0.141.1
uvicorn[standard]==0.53.0
sqlalchemy==2.0.54
alembic==1.20.0
psycopg2-binary==2.9.13
pydantic-settings==2.15.0
pypdf==6.19.0
pyyaml==6.0.3
pyjwt==2.14.0
# Admin 2FA (ADR-0047): TOTP codes, the enrolment QR code as SVG, and Fernet to keep the TOTP
# secret encrypted at rest.
pyotp==2.10.0
segno==1.6.6
cryptography==50.0.1
resend==2.47.0
email-validator==2.3.0
# Releases every time its upstream domain list changes (very often) — bump
# periodically, this pin will go stale fast.
disposable-email-domains==0.0.263
anthropic==1.7.0
# Token-package checkout (ADR-0048): Checkout Sessions and webhook signature verification.
stripe==15.6.1
# Europe/Berlin day boundary for the AI-check budget (app/core/ai_quota.py); slim images have no system tz database.
tzdata==2026.4
# SQLAlchemy needs greenlet only on some platforms (Linux x86_64 yes — Render and CI —, macOS arm64
# no). Listed explicitly so the lock contains it whichever machine it was generated on; without it,
# a lock made on a Mac failed CI's hash-checking install.
greenlet==3.5.6
